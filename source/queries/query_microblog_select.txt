{
    => file
    (
        -< "sunwet/1/file" first {
            => microblog_id
            ( -> "sunwet/1/add_timestamp" first { => add_timestamp } )
            ( -> "sunwet/1/create_timestamp" first { => create_timestamp } )
            ( -> "sunwet/1/url" first { => url } )
            ( -> "sunwet/1/author" first { => author } )
            ( -> "sunwet/1/text" first { => text } )
        }
    )
}